# module: fixture_default_args
# This file is auto-generated by apiary. Do not edit.
# pyright: reportInvalidTypeForm=false

from __future__ import annotations
from typing import Any, Literal, overload
from collections.abc import Callable
import enum
import numpy
import numpy.typing

# %%submodule: 
class Layout(enum.IntEnum):
    RowMajor = 0
    ColMajor = 1

class Order(enum.IntEnum):
    Ascending = 0
    Descending = 1

class Mode(enum.IntEnum):
    Fast = 0
    Exact = 1

class Solver:
    """Owns a nested enum used as a default argument below."""
    def __init__(self) -> None: ...
    def run(self, mode: Mode = ...) -> None:
        """Default argument naming a nested enumerator: the enum's binding must be
        emitted before this method's.
        """
        ...

def arrange(layout: Layout = ...) -> None:
    """Default argument naming a namespace-scope enumerator."""
    ...
def sort_by(order: Order = ...) -> None:
    """Default argument naming an unscoped enumerator - which is spelled `Ascending`
    with no qualification at all. Every enum fixture used the scoped form, whose
    written spelling already carries its enum, so nothing caught that the bare
    name was emitted verbatim into a generated TU at global scope, where it is
    simply undeclared. Found by the fuzz generator on its first run.
    """
    ...
def translate(p: Any, delta: Any = ...) -> None:
    """Default argument that is a template instantiation with a braced initializer.
    Both halves have to survive: the C++ spelling needs the full template-id,
    and the Python rendering has no way to spell it at all.
    """
    ...
def taper(x: float, falloff: float = 1. / 3., offset: int = -1) -> float:
    """Defaults that are expressions rather than values: an arithmetic constant
    fold and a negative number, both of which a naive re-render can mangle.
    """
    ...
def configure(verbose: bool = False, tag: str = "x") -> None:
    """A default-constructed class type, and a bool/char pair - the two literal
    kinds whose Python spelling differs from their C++ spelling.
    """
    ...
