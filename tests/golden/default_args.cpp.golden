// This file is generated by apiary. Do not edit.
//
// Source IR contains 1 class(es), 4 function(s), 1 enum(s).

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/functional.h>
#include <pybind11/numpy.h>
#include <pybind11/operators.h>
#include <pybind11/stl.h>
#include <pybind11/stl/filesystem.h>

namespace py = pybind11;

PYBIND11_MODULE(fixture_default_args, m) {
    py::enum_<einsums::fixture::Layout>(m, "Layout", R"einsums(A scoped enum at namespace scope, for the non-nested comparison.)einsums")
        .value("RowMajor", einsums::fixture::Layout::RowMajor, R"einsums(< C order.)einsums")
        .value("ColMajor", einsums::fixture::Layout::ColMajor, R"einsums(< Fortran order.)einsums");

    py::enum_<einsums::fixture::Solver::Mode>(m, "Mode", R"einsums(How aggressively to converge.)einsums")
        .value("Fast", einsums::fixture::Solver::Mode::Fast, R"einsums(< Stop early.)einsums")
        .value("Exact", einsums::fixture::Solver::Mode::Exact, R"einsums(< Run to convergence.)einsums");

    py::class_<einsums::fixture::Solver>(m, "Solver", R"einsums(Owns a nested enum used as a default argument below.)einsums")
        .def(py::init<>())
        .def("run", static_cast<void (einsums::fixture::Solver::*)(einsums::fixture::Solver::Mode)>(&einsums::fixture::Solver::run),
             py::arg("mode") = einsums::fixture::Solver::Mode::Fast,
             R"einsums(Default argument naming a nested enumerator: the enum's binding must be
emitted before this method's.)einsums");

    m.def("arrange", static_cast<void (*)(einsums::fixture::Layout)>(&einsums::fixture::arrange),
          py::arg("layout") = einsums::fixture::Layout::ColMajor, R"einsums(Default argument naming a namespace-scope enumerator.)einsums");
    m.def("translate",
          static_cast<void (*)(einsums::fixture::Point<double> &, einsums::fixture::Point<double>)>(&einsums::fixture::translate<double>),
          py::arg("p"), py::arg("delta") = einsums::fixture::Point<double>{},
          R"einsums(Default argument that is a template instantiation with a braced initializer.
Both halves have to survive: the C++ spelling needs the full template-id,
and the Python rendering has no way to spell it at all.)einsums");
    m.def("taper", static_cast<double (*)(double, double, int)>(&einsums::fixture::taper), py::arg("x"), py::arg("falloff") = 1. / 3.,
          py::arg("offset") = -1, R"einsums(Defaults that are expressions rather than values: an arithmetic constant
fold and a negative number, both of which a naive re-render can mangle.)einsums");
    m.def("configure", static_cast<void (*)(bool, char)>(&einsums::fixture::configure), py::arg("verbose") = false, py::arg("tag") = 'x',
          R"einsums(A default-constructed class type, and a bool/char pair - the two literal
kinds whose Python spelling differs from their C++ spelling.)einsums");
}
