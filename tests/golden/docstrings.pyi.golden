# module: fixture_docstrings
# This file is auto-generated by apiary. Do not edit.
# pyright: reportInvalidTypeForm=false

from __future__ import annotations
from typing import Any, Literal, overload
from collections.abc import Callable
import enum
import numpy
import numpy.typing

# %%submodule: 
def every_section(value: float, factor: float) -> float:
    """Every section at once.

    A longer explanation that wraps across
    two source lines, to prove the extended summary survives the wrap.

    Parameters
    ----------
    value
        The input value, with a description long enough that it wraps onto a second source line and must be re-indented under its name rather than left dangling.
    factor
        The scale factor.

    Returns
    -------
    The scaled result, described across two lines as well.

    Raises
    ------
    std::invalid_argument
        When ``factor`` is zero.

    Warnings
    --------
    Deprecated. Use ``rescale`` instead.

    Notes
    -----
    .. versionadded:: 1.2.0
    """
    ...
def brief_only(x: float) -> float:
    """Brief only, with nothing else. The docstring must be exactly this line -
    no empty sections, no trailing blank.
    """
    ...
def sparse_params(x: float, y: float) -> float:
    """A parameter documented without a description.

    numpydoc allows a bare name, and an entry that renders as a name with an
    empty indented block underneath is malformed.

    Parameters
    ----------
    x
    y
        The second one, which does have a description.
    """
    ...
def bare_deprecated(x: float) -> float:
    """Deprecated with no migration note.

    The Warnings section still has to say something; an empty admonition is
    worse than none.

    Warnings
    --------
    Deprecated.
    """
    ...
def rest_in_prose(x: float) -> float:
    """Prose that is itself reST.

    Doc comments may contain reST by design, and a docstring is read as reST by
    every tool that renders one, so blocks must survive into it intact.

    .. code-block:: py

       scaled = every_section(2.0, 3.0)

    .. note::

       An admonition, which becomes a directive in the docstring too.

    Parameters
    ----------
    x
        The value.
    """
    ...
def with_math(x: float) -> float:
    r"""Math, which is LaTeX, which is backslashes.

    A docstring is a Python string literal, so `\sum` in it is an escape
    sequence Python does not know. Since 3.12 that is a SyntaxWarning that says
    it "will not work in the future", which puts the stub on a path to not
    parsing at all. The literal has to be raw.

    The contraction is :math:`c_{ik} = \sum_j a_{ij} b_{jk}`, and as a block:

    .. math::

       \alpha \sum_k \mathrm{tr}(A_k)

    Parameters
    ----------
    x
        The value.
    """
    ...
def only_a_dropped_command(x: float) -> float:
    ...
