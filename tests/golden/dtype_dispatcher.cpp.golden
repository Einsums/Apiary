// This file is generated by apiary. Do not edit.
//
// Source IR contains 1 class(es), 2 function(s), 0 enum(s).

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/functional.h>
#include <pybind11/numpy.h>
#include <pybind11/operators.h>
#include <pybind11/stl.h>
#include <pybind11/stl/filesystem.h>

namespace py = pybind11;

PYBIND11_MODULE(fixture_dtype_dispatcher, m) {
    py::class_<einsums::fixture::Allocator>(m, "Allocator",
                                            R"einsums(The member form, which is the shape that actually shipped broken.)einsums")
        .def(py::init<>())
        .def(
            "allocate",
            [](einsums::fixture::Allocator &self, int slot, bool intermediate, std::string const &dtype) -> py::object {
                if (dtype == "float32" || dtype == "float" || dtype == "f4" || dtype == "f" || dtype == "single") {
                    return py::cast((self.*static_cast<float (einsums::fixture::Allocator::*)(int, bool)>(
                                               &einsums::fixture::Allocator::allocate))(slot, intermediate));
                }
                if (dtype == "float64" || dtype == "double" || dtype == "f8" || dtype == "d") {
                    return py::cast((self.*static_cast<double (einsums::fixture::Allocator::*)(int, bool)>(
                                               &einsums::fixture::Allocator::allocate))(slot, intermediate));
                }
                throw py::value_error("unsupported dtype: " + dtype);
            },
            py::arg("slot"), py::arg("intermediate") = true, py::arg("dtype") = "float64",
            R"einsums(Allocate a named buffer. `intermediate` has a default and precedes the
dtype selector, exactly as Graph::create_tensor does.)einsums");

    m.def(
        "zero_value",
        [](int seed, std::string const &dtype) -> py::object {
            if (dtype == "float32" || dtype == "float" || dtype == "f4" || dtype == "f" || dtype == "single") {
                return py::cast(einsums::fixture::zero_value<float>(seed));
            }
            if (dtype == "float64" || dtype == "double" || dtype == "f8" || dtype == "d") {
                return py::cast(einsums::fixture::zero_value<double>(seed));
            }
            throw py::value_error("unsupported dtype: " + dtype);
        },
        py::arg("seed"), py::arg("dtype") = "float64", R"einsums(Allocate-and-zero a scalar value, returned as the dtype's value
type. The four INSTANTIATE_AS lines all share the same (int) arg
signature and differ only in the return type, so the codegen
auto-detects the dtype-dispatcher pattern.)einsums");
    m.def(
        "scaled_zero",
        [](int seed, bool normalize, std::string const &dtype) -> py::object {
            if (dtype == "float32" || dtype == "float" || dtype == "f4" || dtype == "f" || dtype == "single") {
                return py::cast(einsums::fixture::scaled_zero<float>(seed, normalize));
            }
            if (dtype == "float64" || dtype == "double" || dtype == "f8" || dtype == "d") {
                return py::cast(einsums::fixture::scaled_zero<double>(seed, normalize));
            }
            throw py::value_error("unsupported dtype: " + dtype);
        },
        py::arg("seed"), py::arg("normalize") = true, py::arg("dtype") = "float64",
        R"einsums(Free-function dispatcher with a defaulted argument ahead of the selector.)einsums");
}
