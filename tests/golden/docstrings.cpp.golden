// This file is generated by apiary. Do not edit.
//
// Source IR contains 0 class(es), 6 function(s), 0 enum(s).

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/functional.h>
#include <pybind11/numpy.h>
#include <pybind11/operators.h>
#include <pybind11/stl.h>
#include <pybind11/stl/filesystem.h>

namespace py = pybind11;

PYBIND11_MODULE(fixture_docstrings, m) {
    m.def("every_section", static_cast<double (*)(double, double)>(&einsums::fixture::every_section), py::arg("value"), py::arg("factor"),
          R"einsums(Every section at once.

A longer explanation that wraps across
two source lines, to prove the extended summary survives the wrap.

Parameters
----------
value
    The input value, with a description long enough that it wraps onto a second source line and must be re-indented under its name rather than left dangling.
factor
    The scale factor.

Returns
-------
The scaled result, described across two lines as well.

Raises
------
std::invalid_argument
    When ``factor`` is zero.

Warnings
--------
Deprecated. Use ``rescale`` instead.

Notes
-----
.. versionadded:: 1.2.0)einsums");
    m.def("brief_only", static_cast<double (*)(double)>(&einsums::fixture::brief_only), py::arg("x"),
          R"einsums(Brief only, with nothing else. The docstring must be exactly this line -
no empty sections, no trailing blank.)einsums");
    m.def("sparse_params", static_cast<double (*)(double, double)>(&einsums::fixture::sparse_params), py::arg("x"), py::arg("y"),
          R"einsums(A parameter documented without a description.

numpydoc allows a bare name, and an entry that renders as a name with an
empty indented block underneath is malformed.

Parameters
----------
x
y
    The second one, which does have a description.)einsums");
    m.def("bare_deprecated", static_cast<double (*)(double)>(&einsums::fixture::bare_deprecated), py::arg("x"),
          R"einsums(Deprecated with no migration note.

The Warnings section still has to say something; an empty admonition is
worse than none.

Warnings
--------
Deprecated.)einsums");
    m.def("rest_in_prose", static_cast<double (*)(double)>(&einsums::fixture::rest_in_prose), py::arg("x"),
          R"einsums(Prose that is itself reST.

Doc comments may contain reST by design, and a docstring is read as reST by
every tool that renders one, so blocks must survive into it intact.

.. code-block:: py

   scaled = every_section(2.0, 3.0)

.. note::

   An admonition, which becomes a directive in the docstring too.

Parameters
----------
x
    The value.)einsums");
    m.def("only_a_dropped_command", static_cast<double (*)(double)>(&einsums::fixture::only_a_dropped_command), py::arg("x"));
}
