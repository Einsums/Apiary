// This file is generated by apiary. Do not edit.
//
// Source IR contains 2 class(es), 3 function(s), 0 enum(s).

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/functional.h>
#include <pybind11/numpy.h>
#include <pybind11/operators.h>
#include <pybind11/stl.h>
#include <pybind11/stl/filesystem.h>

namespace py = pybind11;

PYBIND11_MODULE(fixture_cross_module, m) {
    // Submodules declared via @module directives.
    auto _sub_core   = m.def_submodule("core");
    auto _sub_shapes = m.def_submodule("shapes");
    auto _sub_util   = m.def_submodule("util");

    py::class_<einsums::fixture::Shape>(_sub_core, "Shape", R"einsums(A base bound into the ``core`` submodule.)einsums")
        .def(py::init<>())
        .def("area", static_cast<double (einsums::fixture::Shape::*)() const>(&einsums::fixture::Shape::area),
             R"einsums(Area of the shape.)einsums");

    py::class_<einsums::fixture::Circle, einsums::fixture::Shape>(
        _sub_shapes, "Circle", R"einsums(A derived class bound into a DIFFERENT submodule from its base. The base's
binding has to be emitted first even though it belongs to another module
object.)einsums")
        .def(py::init<>())
        .def("area", static_cast<double (einsums::fixture::Circle::*)() const>(&einsums::fixture::Circle::area),
             R"einsums(Area of the circle.)einsums");

    _sub_util.def("enclosing_circle",
                  static_cast<einsums::fixture::Circle (*)(einsums::fixture::Shape const &)>(&einsums::fixture::enclosing_circle),
                  py::arg("shape"), R"einsums(A free function in a third submodule taking the base by reference and
returning the derived type, so both names must resolve from a module that
declares neither.)einsums");
    m.def("undocumented", static_cast<double (*)(double)>(&einsums::fixture::undocumented), py::arg("x"));
    m.def("rst_only", static_cast<double (*)(double)>(&einsums::fixture::rst_only), py::arg("x"),
          R"einsums(Documented only through an embedded reST block, with no prose of its own.
The whole doc comment is one protected span, so the brief is produced
entirely by splicing rather than by any of the line handling.

.. note::

A directive whose body should stay indented, and does not.)einsums");
}
